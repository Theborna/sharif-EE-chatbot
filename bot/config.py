import os
from dotenv import load_dotenv

# Load environment variables from .env file
load_dotenv()

class Config:
    # API Configuration
    API_BASE_URL = os.getenv("API_BASE_URL", "http://localhost:8020")
    API_USERNAME = os.getenv("API_USERNAME", "admin")
    API_PASSWORD = os.getenv("API_PASSWORD", "admin")
    
    # Bot Configuration
    WELCOME_TEXT = """
🤖 سلام!
من دستیار هوش مصنوعی دانشکده برق شریف هستم و می‌توانم به شما کمک کنم.
مستقیماً از تلگرام با من گفتگو کنید!

من با یک مدل زبان بزرگ (LLM) سفارشی پشتیبانی می‌شوم و می‌توانم به سوالات شما به فارسی و انگلیسی پاسخ دهم.

🛠️ دستورات موجود:
• <code>/start</code>, <code>/help</code> - نمایش این پیام خوش‌آمدگویی
• <code>/ping</code> - بررسی زمان پاسخ‌دهی ربات
• <code>/report &lt;پیام&gt;</code> - ارسال گزارش به مدیران
• <code>/ask &lt;سوال&gt;</code> - هر چیزی که می‌خواهید از من بپرسید! (فقط در چت‌های گروهی لازم است)
• <code>/new_chat</code> - شروع یک گفتگوی جدید (پاک شدن تاریخچه قبلی)
• <code>/end_chat</code> - پایان گفتگوی فعلی شما
• <code>/enable_memory</code> - فعال کردن حافظه برای این گفتگو
• <code>/disable_memory</code> - غیرفعال کردن حافظه برای این گفتگو

💡 <b>نکته:</b> در پیام‌های خصوصی می‌توانید مستقیماً با من صحبت کنید و نیازی به <code>/ask</code> نیست. در چت‌های گروهی باید از <code>/ask &lt;سوال&gt;</code> استفاده کنید.

💡 <b>حالت اینلاین:</b> شما می‌توانید من را در هر چت دیگری با تایپ کردن
<code>@sharif_EE_chatbot</code> و سپس سوال خود استفاده کنید!
هنگامی که تایپ کردن را متوقف کنید، یک پنجره "پرسیدن سوال" ظاهر می‌شود.
روی آن کلیک کنید تا سوال ارسال شود و پاسخ همان‌جا نمایش داده شود.

فقط یک پیام برای من بفرستید و من سعی می‌کنم کمک کنم! 🚀
    """

    CURRENT_APIS = ["Custom LLM", "Ping Service", "Report System", "Inline Queries"]
    
    # Directories
    REPORTS_DIR = "./reports"
    
    @classmethod
    def ensure_directories(cls):
        """Create necessary directories if they don't exist"""
        os.makedirs(cls.REPORTS_DIR, exist_ok=True)
